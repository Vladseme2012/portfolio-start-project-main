import styled from 'styled-components';

const GoTopBtn = styled.button`
    background-color: rgba(0, 0, 0, 0.3);
    padding: 8px;
    position: fixed;
    right: 30px;
    bottom: 30px;
`;

export const S = {
    GoTopBtn,
};
