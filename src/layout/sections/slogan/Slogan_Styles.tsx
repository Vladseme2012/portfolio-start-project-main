import styled from 'styled-components';

const Slogan = styled.section`
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 100%;
    min-height: 200px;
`;

export const S = {
    Slogan,
};
